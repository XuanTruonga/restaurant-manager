export const modalSelectorPrimary = (state) => {
  return state.modalPrimary;
};

export const modalSelectorSecondary = (state) => {
  return state.modalSecondary;
};

export const modalSelectorUpdate = (state) => {
  return state.modalUpdate;
};

export const modalSelectorDetail = (state) => {
  return state.modalDetail;
};
